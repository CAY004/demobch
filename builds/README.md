# Build Outputs

Thư mục này để chứa bản build khi đóng gói:
- `builds/apk/`: Android APK build artifacts
- `builds/exe/`: Windows EXE build artifacts

Lưu ý: môi trường hiện tại chưa chạy quy trình build Android/Windows native. Đây là cấu trúc sẵn để bạn copy file build vào.
